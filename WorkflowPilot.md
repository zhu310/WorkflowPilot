# WorkflowPilot：基于大模型的智能流程图编辑系统

## 一、项目背景

我们希望设计一个可以通过自然语言编辑复杂流程画布的 AI 系统。

用户面对的不是一段普通文本，而是一张由大量节点、连线、分组和条件组成的结构化画布。用户可以先选中部分内容，也可以直接通过自然语言描述自己的目标，例如：

- “在这里增加一个人工审核步骤。”
- “把选中的节点排整齐一些。”
- “这一部分太拥挤了，帮我调整一下，但不要影响右边的发布流程。”
- “所有失败分支最后都要进入人工处理。”
- “当前流程太线性了，把数据准备和权限检查改成并行执行。”
- “重新组织整张画布，让主流程更清晰，同时保留所有已有功能。”
- “刚才调整得太分散了，紧凑一些。”
- “帮我找出流程中可能永远无法到达的节点，并给出修改方案。”

系统需要理解用户意图，并输出能够被程序读取的结构化画布结果或结构化修改结果。不能只返回一段文字建议。

本题不要求实现一个完整的在线流程图产品。你可以使用网页、桌面界面、Notebook、命令行程序、生成的 HTML/SVG、Mermaid、Graphviz 或其他形式完成演示，但必须满足：

1. 底层输入和输出数据可以被查看；
2. 修改前后的结果可以通过某种可视化形式进行比较；
3. 能够说明系统实际修改了什么，而不只是展示最终图片。

---

## 二、基础节点目录

系统至少应认识下面这些节点。你不必实现所有节点的完整业务功能，只需要让它们在流程语义、结构数据和可视化上具有明确含义。

### 1. 流程控制节点

| 节点类型 | 含义 |
|---|---|
| `start` | 流程入口 |
| `end` | 流程结束 |
| `decision` | 根据条件进入不同分支 |
| `parallel_split` | 将流程拆分成多个并行分支 |
| `parallel_join` | 等待多个并行分支汇合 |
| `subprocess` | 引用一个独立子流程 |
| `loop` | 表示满足条件前重复执行一段流程 |
| `wait` | 等待指定事件、时间或外部条件 |

### 2. 执行节点

| 节点类型 | 含义 |
|---|---|
| `task` | 普通任务或处理步骤 |
| `ai_agent` | 由 AI Agent 负责的任务 |
| `tool` | 调用外部工具或能力 |
| `human_review` | 需要人工判断、确认或审批 |
| `notification` | 发送消息或通知 |
| `checkpoint` | 检查流程阶段性结果 |

### 3. 数据与资源节点

| 节点类型 | 含义 |
|---|---|
| `data_source` | 数据来源 |
| `document` | 文档、规则或知识材料 |
| `storage` | 数据保存位置 |
| `variable` | 流程中使用的变量或状态 |
| `resource` | 某个步骤依赖的资源 |

### 4. 辅助节点

| 节点类型 | 含义 |
|---|---|
| `note` | 说明和备注，不参与流程执行 |
| `group` | 表示一个阶段、模块或逻辑区域 |
| `milestone` | 关键里程碑 |
| `error_handler` | 统一处理异常或失败分支 |

求职者可以自行增加新的节点类型、功能块和交互方式。扩展内容应当服务于真实使用体验，而不是单纯增加数量。

---

## 三、参考数据结构

以下结构只用于说明输入中可能包含的信息。你可以直接使用，也可以提出更合理的设计，但需要说明原因。

### 1. 节点

```json
{
  "id": "node_014",
  "type": "human_review",
  "title": "人工复核",
  "description": "当自动检查无法确认时进入人工复核",
  "position": {
    "x": 860,
    "y": 420
  },
  "size": {
    "width": 180,
    "height": 72
  },
  "group_id": "stage_02",
  "config": {
    "owner": "quality_team",
    "timeout_minutes": 30
  }
}
```

### 2. 连线

```json
{
  "id": "edge_021",
  "source": "node_010",
  "target": "node_014",
  "label": "检查失败",
  "condition": "score < 0.8"
}
```

### 3. 分组

```json
{
  "id": "stage_02",
  "title": "质量检查阶段",
  "position": {
    "x": 700,
    "y": 200
  },
  "size": {
    "width": 760,
    "height": 520
  }
}
```

### 4. 一次编辑请求

```json
{
  "instruction": "把选中的检查流程调整得清晰一些，失败时统一进入人工复核，其他阶段不要修改",
  "graph": {
    "nodes": [],
    "edges": [],
    "groups": []
  },
  "selected_node_ids": [
    "node_009",
    "node_010",
    "node_011"
  ],
  "visible_area": {
    "left": 600,
    "right": 1600,
    "top": 100,
    "bottom": 900
  },
  "conversation_history": []
}
```

---

## 四、需要完成的任务

请从零设计并实现一个可演示的 AI 智能流程画布编辑原型。

你可以自由选择模型、框架和实现方式，也可以自行决定使用单次模型调用、Agent、固定流程、多阶段处理或其他方案。题目不预设标准架构。

### 任务 1：结构化读取与输出

系统应当能够：

- 接收结构化画布数据；
- 接收用户的自然语言编辑要求；
- 输出新的完整画布，或者输出一组可以作用于原画布的结构化修改；
- 展示修改前后的数据差异；
- 保证输出可以被程序解析，而不是仅输出自然语言说明。

你需要自行定义或完善输出协议。

### 任务 2：完成基础编辑

至少演示以下类型中的四种：

- 新增或删除节点；
- 移动一个或多个节点；
- 修改节点属性；
- 创建、删除或修改连线；
- 对齐或均匀分布选中节点；
- 创建或调整流程分组；
- 将串行步骤改成并行结构；
- 为失败分支补充统一处理节点。

### 任务 3：理解不同修改范围

系统需要能够面对以下不同范围的请求：

- 只修改一个明确节点；
- 只修改用户选中的节点；
- 调整当前可见区域；
- 修改一个完整阶段或子流程；
- 对整张画布进行全局调整；
- 用户没有明确说明范围，需要系统进行合理判断。

请展示至少一个局部编辑案例和一个全局编辑案例，并说明系统最终实际修改了哪些内容。

### 任务 4：处理抽象和复杂要求

除明确的坐标或节点操作外，系统还需要尝试处理更接近真实用户表达的要求，例如：

- “让主要流程更突出。”
- “这一阶段太复杂了，简化一下，但不要减少检查步骤。”
- “把流程调整得循序渐进一些。”
- “减少不必要的来回跳转。”
- “正常情况尽量自动完成，只有不确定时才交给人工。”
- “保留原有功能，但让整个流程更容易理解和维护。”

请选择至少两个抽象要求进行演示。需要展示的不只是最终图，还包括系统对需求的结构化理解、产生的实际变化以及最终结果。

### 任务 5：支持连续编辑

至少演示一次连续两轮或三轮修改，例如：

```text
用户：把当前检查阶段整理清楚一点。
用户：还是太分散了，紧凑一些。
用户：第二个检查节点不要动，恢复到原来的位置。
```

系统需要合理理解后续表达中的指代、范围以及用户对上一轮结果的修正。

### 任务 6：结果可视化

你不需要开发完整的流程图排版网站，但至少需要提供一种可重复查看的可视化结果，例如：

- 生成可打开的 HTML；
- 生成 SVG 或图片；
- 使用 Mermaid 或 Graphviz；
- 在 Notebook 中绘制；
- 使用已有画布组件制作简单界面；
- 提供修改前后对照视图。

可视化结果需要让评审者能够辨认：

- 节点类型；
- 节点之间的连接关系；
- 本次修改影响了哪些内容；
- 修改前后有什么区别。

---

## 五、复杂流程图挑战

基础流程图的数据量和输入长度较小，仅仅完成简单排版不能充分体现系统能力。

请额外设计一个复杂流程图测试。画布可以由程序生成，也可以由公开数据或自行设计的数据构造。

复杂测试建议包含：

- 至少 300 个节点；
- 多个阶段或子流程；
- 条件分支、并行分支和汇合关系；
- 多种不同节点类型；
- 部分结构位于当前可见区域之外；
- 至少一个只允许局部修改的任务；
- 至少一个需要理解整体结构的任务；
- 至少一个表达不完整或存在一定歧义的用户要求。

复杂流程图场景下，请重点展示：

1. 系统是否仍能正确理解修改目标；
2. 局部修改是否影响了无关区域；
3. 全局修改是否真正考虑了整体结构；
4. 输出结果是否保持结构完整；
5. 系统在输入规模增大后，效果、耗时和成本发生了什么变化；
6. 当信息不足时，系统作出了怎样的处理。

如果系统能够稳定处理 1000 个以上节点、跨多个子流程的连续编辑，属于明显加分项。

---

## 六、最低正确性要求

无论采用什么架构，最终结果至少不应出现以下明显问题：

- 节点 ID 重复；
- 连线指向不存在的节点；
- 未经用户要求大量删除原有内容；
- 局部编辑无故改变不相关区域；
- 节点发生大量明显重叠；
- 用户要求保留的节点或流程被删除；
- 系统声称完成了某项修改，但底层数据没有对应变化；
- 输出无法被程序稳定解析；
- 连续编辑基于错误或已经过期的画布状态。

你可以自行增加更多检查规则，并说明为什么需要这些规则。

---

## 七、必须演示的测试用例

最终演示至少包含以下八类用例：

1. 一个明确的单节点修改；
2. 一个选中多个节点后的局部排列修改；
3. 一个新增节点并补充连线的请求；
4. 一个局部抽象优化请求；
5. 一个全局重组请求；
6. 一个连续多轮修正请求；
7. 一个表达存在歧义、系统不能直接可靠执行的请求；
8. 一个包含至少 300 个节点的复杂画布请求。

每个用例需要提供：

- 原始用户输入；
- 修改前数据或数据摘要；
- 系统输出的结构化结果；
- 修改前后可视化；
- 实际变化摘要；
- 你认为结果是否正确，以及判断依据。

---

## 八、提交物

请提交：

### 1. 可运行或可重复演示的原型

应提供清晰的启动或运行方式。若依赖外部模型，请说明配置方法；不要提交真实密钥。

### 2. 项目说明文档

至少说明：

- 你对问题的理解；
- 完整处理流程；
- 主要模块及其职责；
- 大模型负责什么、不负责什么；
- 如何处理局部、全局和模糊请求；
- 如何支持多轮修改；
- 如何发现并处理错误结果；
- 当前方案的限制；
- 如果继续迭代，下一步优先改进什么。

### 3. 示例与测试结果

包含本题要求的八类用例，以及你自行增加的测试。

### 4. 可视化产物

提交能够查看修改前后结果的 HTML、SVG、图片、Notebook 或其他产物。

### 5. AI 使用说明

允许并鼓励使用 AI 辅助开发。请简要说明：

- AI 帮助你完成了哪些工作；
- 哪些关键设计由你决定；
- 你如何验证 AI 生成内容的正确性；
- 你曾否定或修改过哪些 AI 方案，为什么。

我们不以手写代码量评价能力，更关注你是否能够定义问题、设计方案、判断结果并对最终质量负责。

---

## 九、评价重点

我们会重点关注以下方面：

### 1. 实际效果

系统是否真正完成用户要求，而不是只生成看起来合理的解释。

### 2. 复杂意图理解

系统能否处理抽象表达、指代、省略、连续修改，以及局部目标和整体目标之间的关系。

### 3. 复杂画布处理能力

系统是否只适用于几十个节点的小例子，还是能够处理较大的真实流程图。

### 4. 修改准确性

该修改的内容是否修改，不该修改的内容是否保持稳定。

### 5. 结果可信度

系统是否能发现无效结构、错误连线、越界修改、遗漏要求或实际结果与文字描述不一致的问题。

### 6. 产品体验

用户能否理解系统准备做什么、实际做了什么，以及如何继续调整、确认、拒绝或恢复修改。

### 7. 架构取舍

方案是否与问题规模相匹配，能否说明准确率、延迟、成本、复杂度和可维护性之间的取舍。

### 8. 测试与迭代能力

测试是否覆盖真实失败情况，是否能够根据失败案例定位问题并继续改进，而不是只展示少量成功示例。

---

## 十、加分项

以下内容均可作为加分项，但不要求全部实现：

- 支持 1000 个以上节点的复杂画布；
- 支持按阶段、节点类型、上下游关系或其他条件进行大范围修改；
- 能够合理推测用户省略的信息，并在不可靠时避免擅自执行；
- 能够展示修改计划、修改预览和结构化差异；
- 支持撤销、恢复、版本比较或基于上一版本继续编辑；
- 对修改范围、重叠、断链、孤立节点、循环和不可达节点进行检查；
- 能够在复杂任务中逐步获取所需信息，而不是受限于一次输入长度；
- 能够解释为什么修改这些节点、为什么没有修改其他节点；
- 能够将成功的处理经验沉淀为可复用能力；
- 提供自动化评测方法，而不是完全依靠人工观察图片；
- 扩展出有实际价值的新节点、新编辑能力或更好的交互方式；
- 在效果、响应时间和模型成本之间作出合理优化。

功能丰富是加分项，但前提是已有能力真实可用、结果可信、产品体验清楚。大量不可验证或只能演示固定样例的功能不会获得额外评价。

---

## 十一、限制与说明

- 要求python为基础语言实现核心代码逻辑；
- 不要求实现用户登录、权限系统、数据库管理等外围功能；
- 不以界面视觉精美程度作为主要评价标准；
- 可以使用 AI 编写代码，但需要能够解释、验证和改进最终方案；
- 请勿提交只能针对题目示例返回固定答案的实现；
- 如因时间限制只完成部分内容，请明确说明完成范围、未完成部分及后续方案。

本题没有唯一标准答案。我们希望看到的不是特定框架名称，而是你面对真实、复杂、存在歧义的 AI 编辑任务时，如何分析问题、设计系统、验证结果并持续改进。
